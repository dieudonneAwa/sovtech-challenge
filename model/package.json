{
  "name": "model",
  "version": "1.0.0",
  "description": "These are type definitions to be used across the project, in both client and server code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
